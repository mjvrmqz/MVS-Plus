const video = { id: 'HEa9vzq4g9E', name: 'Avatar 1', subs: '200k Subscribers' };

const nameChars = video.name.split('');
const subsChars = video.subs.split('');
const charDelay = 0.04;
const nameStartDelay = 0.1;
const subsStartDelay = nameStartDelay + nameChars.length * charDelay + 0.1;
---

<section id="work" class="bottom">
  <div class="bottom__inner">
    <div class="bottom__row">
      <div class="bottom__video-wrap reveal">
        <div class="bottom__video-panel" aria-hidden="true"></div>
        <div class="bottom__video">
          <iframe
            src={`https://www.youtube.com/embed/${video.id}`}
            title={`${video.name} — featured edit`}
            frameborder="0"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
            loading="lazy"
          ></iframe>
        </div>
      </div>

      <div class="bottom__label">
        <p class="bottom__name" aria-label={video.name}>
          {nameChars.map((char, i) => (
            <span
              class="bottom__char reveal"
              style={`--reveal-delay:${(nameStartDelay + i * charDelay).toFixed(3)}s`}
              aria-hidden="true"
            >{char === ' ' ? '\u00A0' : char}</span>
          ))}
        </p>
        <p class="bottom__subs" aria-label={video.subs}>
          {subsChars.map((char, i) => (
            <span
              class="bottom__char reveal"
              style={`--reveal-delay:${(subsStartDelay + i * charDelay).toFixed(3)}s`}
              aria-hidden="true"
            >{char === ' ' ? '\u00A0' : char}</span>
          ))}
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .bottom {
    width: 100%;
    background: linear-gradient(180deg, #441378 0%, #3B1265 100%);
    padding: 120px 0 140px;
    position: relative;
    overflow: visible;
  }

  .bottom__inner {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 48px;
  }

  .bottom__row {
    position: relative;
    display: flex;
    align-items: center;
    gap: 80px;
  }

  .bottom__video-wrap {
    position: relative;
    width: 100%;
    max-width: 860px;
    flex: 1.3;
    flex-shrink: 0;
    z-index: 2;
  }

  .bottom__video-panel {
    position: absolute;
    top: -6%;
    bottom: -6%;
    left: 0;
    right: -4%;
    background: var(--color-bg-panel);
    clip-path: polygon(0% 0%, 60% 0%, 100% 100%, 0% 100%);
    z-index: 0;
  }

  .bottom__video {
    position: relative;
    width: 100%;
    aspect-ratio: 383 / 203;
    border-radius: 20px;
    overflow: hidden;
    background: #000;
    z-index: 1;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.35);
  }

  .bottom__video iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  .bottom__label {
    position: relative;
    flex: 1;
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 0;
    z-index: 2;
  }

  .bottom__name {
    margin: 0;
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(2.8rem, 5.5vw, 5rem);
    line-height: 1.0;
    color: var(--color-white);
  }

  .bottom__subs {
    margin: 0;
    font-family: 'Mollen Regular', 'Mollen', sans-serif;
    font-weight: 400;
    font-size: clamp(1.5rem, 3.5vw, 2.4rem);
    line-height: 1.0;
    color: var(--color-cream);
  }

  .bottom__char {
    display: inline-block;
  }

  .bottom__char.reveal {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 0.55s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.55s cubic-bezier(0.22, 1, 0.36, 1);
    transition-delay: var(--reveal-delay, 0s);
  }

  .bottom__char.reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (max-width: 760px) {
    .bottom {
      padding: 80px 0 96px;
    }

    .bottom__inner {
      padding: 0 24px;
    }

    .bottom__row {
      flex-direction: column;
      gap: 32px;
    }

    .bottom__video-wrap {
      max-width: 100%;
    }

    .bottom__video-panel {
      left: -3%;
      right: -3%;
      clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%);
    }
  }
</style>
