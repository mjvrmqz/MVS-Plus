#!/usr/bin/env python3
# cookie_patch.py
# Partial helper — see full X Keyword Scraper for usage context

# cookies.append({
#     "name": name,
#     "value": value,
#     "domain": host if host.startswith(".") else "." + host.lstrip("."),
#     "path": path if path else "/",
#     "secure": bool(secure),
#     "httpOnly": bool(httponly),
# })
